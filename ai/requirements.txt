# AI Module Dependencies for WhisperFlow

# Stage 1: Denoising (PyTorch CRNN)
torch>=2.0.0
torchaudio>=2.0.0
librosa>=0.10.0
soundfile>=0.12.1

# Stage 2: VAD (Silero ONNX)
onnxruntime>=1.15.0

# Stage 3: Transcription & Correction (Whisper / LLM)
# Using faster-whisper (CTranslate2) instead of raw transformers for massive speedup
faster-whisper>=1.0.0
opencc-python-reimplemented>=0.1.7
